[package]
name = "bob-protocol"
version.workspace = true
edition.workspace = true
description = "Wire protocol shared by the bob relay and remote host."

[lib]
name = "bob_protocol"
path = "src/lib.rs"

[dependencies]
bob-core = { path = "../core" }
serde.workspace = true
serde_json.workspace = true
